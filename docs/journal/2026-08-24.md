# 2026-08-24

## Fait

- **Hotfix des médias de slides en 404 en production**
  ([IFO-017](../tickets/IFO-017-storage-link-non-root.md)) : régression du
  conteneur non-root d'IFO-015. `artisan storage:link` tournait en `www-data`
  contre un `/app/public` possédé par root : le lien `public/storage` n'était
  plus recréé au démarrage et son échec était avalé par `|| true` — tous les
  `/storage/*` répondaient 404, y compris les fichiers plus anciens (intacts sur
  le volume). Correctif : le lien est posé en `ln -sfn` dans la phase root de
  `docker-entrypoint.sh`, avant le `setpriv`. Docker local indisponible (WSL2 en
  réinstallation) : pas de rejeu prod-like local, poussé sur `main` pour
  redéploiement Coolify automatique après relecture et `bash -n`. Contournement
  immédiat possible dans le conteneur de prod :
  `ln -sfn /app/storage/app/public /app/public/storage`.

- **Retours de la directrice**
  ([IFO-018](../tickets/IFO-018-retours-directrice.md)), branche
  `feat/retours-directrice`, quatre commits :
  - tri alphabétique des enseignants (index + selects des formulaires de
    cours), des cours et des sections (par nom) partout ; tri naturel des
    locaux consolidé dans `resources/js/lib/rooms.ts` avec correction du bug
    `parseInt` sur les sous-sols nommés « - 103 » (espace après le tiret) ;
  - année d'étude facultative sur les cours (1re/2e/3e), migration
    `add_year_to_courses_table`, validée `in:1,2,3`, visible dans les
    formulaires, la fiche, la liste et le planning de l'écran TV (sous-titre
    « code · 1ère année ») ;
  - ratio des médias conservé : slides image/vidéo de l'écran et aperçus de
    `/screen/slides` en `object-contain` sur fond `#1e2d55` (bleu du branding) ;
  - `CourseCatalogSeeder` manuel (`php artisan db:seed
    --class=CourseCatalogSeeder`) : catalogue IFOSUP relevé par Thibault,
    œnologie regroupée en une section, cours déjà saisis à la main exclus,
    idempotent (firstOrCreate par code, syncWithoutDetaching), 4 tests.
  - Suite complète verte dans le container dev : **293 tests**, 1159 assertions ;
    Pint, ESLint, Prettier et vue-tsc OK. Seeder rejoué en dev : 20 sections,
    219 cours.

## Leçon

- Un `|| true` sur une commande de démarrage a masqué la régression : l'échec de
  `storage:link` aurait dû faire du bruit. Les vérifications d'IFO-015 avaient
  testé l'écriture dans `storage/`, pas le service des médias via
  `public/storage` — ajouter ce contrôle à toute recette qui touche l'image.
