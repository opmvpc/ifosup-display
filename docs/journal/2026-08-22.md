# 2026-08-22

## Fait

- **Lancement de l'audit croisé de la reprise** ([IFO-013](../tickets/IFO-013-audit-croise-reprise.md)) :
  quatre auditeurs indépendants sur des périmètres disjoints — sécurité applicative
  (Fable), review des commits de la reprise (Fable), infra Docker/Coolify/CI (Opus),
  review croisée externe (Codex GPT-5.6 Sol en read-only). L'orchestrateur a
  contre-vérifié par sondage les correctifs les plus risqués de la reprise
  (migrations de dédoublonnage, transaction d'import, plages horaires de
  `ScreenController`) : conformes à ce qu'annonce STATUS.
- **Traduction française du starter kit** ([IFO-012](../tickets/IFO-012-traduction-fr-starter-kit.md)) :
  pages Paramètres (profil, mot de passe, apparence), suppression de compte,
  confirmation de mot de passe, menu utilisateur, onglets d'apparence, et chaînes
  d'accessibilité `sr-only` des composants ui. Le reste de l'application était déjà
  en français.

## Résultats des audits

Verdict d'ensemble : **la reprise d'Opus est fiable** — détail dans
[IFO-013](../tickets/IFO-013-audit-croise-reprise.md) et les quatre rapports de
`docs/research/`.

- **Sécurité applicative (Fable)** — rien de bloquant. Deux points importants
  confirmés par relecture orchestrateur :
  1. `config/session.php:172` : `SESSION_SECURE_COOKIE` n'est définie nulle part et
     aucun trusted proxy n'est configuré dans `bootstrap/app.php` → le cookie de
     session part sans flag `Secure` derrière le proxy Coolify.
  2. `Admin/UserController::destroy()` : aucun garde-fou — un utilisateur peut
     supprimer son propre compte ou le dernier compte existant, or l'inscription et
     le reset par email sont désactivés → lockout complet du backoffice.
  Mineurs : `/screen/data` sans throttle, pas d'en-têtes de sécurité HTTP, import
  Excel DoS-able par fichier piégé (authentifié), sessions non invalidées au
  changement de mot de passe. Sain par ailleurs : pas de secret committé, `$fillable`
  stricts, pas de SQL brut injectable, pas de XSS, uploads validés.
- **Review des commits (Fable)** — chaque fix revendiqué vérifié réel à HEAD ; CI
  réellement verte (274 tests, 1094 assertions, PHP 8.4/8.5). Réserves mineures :
  `store`/`update` de créneau font un 500 (au lieu de 422) sur course résiduelle,
  compteurs de STATUS périmés d'une unité (corrigés ce jour).
- **Infra Docker/Coolify/CI (Opus)** — trois angles morts importants : **aucun
  `php.ini` actif dans l'image** (uploads plafonnés aux 2 Mo compilés de PHP, alors
  que les slides acceptent des vidéos de 300 Mo — la fonctionnalité était cassée en
  prod), pas de trusted proxies, logs applicatifs invisibles depuis Coolify
  (`LOG_CHANNEL=stack` vers un fichier sans rotation). Plus : conteneur en root,
  `migrate --force` sans verrou (sain à 1 replica), sonde MySQL aveugle à un
  `DB_PASSWORD` changé après coup, écarts de doc (port dev 8001, commande de dump,
  politique `ADMIN_PASSWORD` non appliquée par la commande).
- **Review croisée externe (Codex Sol)** — les meilleures prises de la session,
  toutes contre-vérifiées : la migration renommée par `073e159` casse la mise à
  niveau des bases pré-reprise (résurrection de `recurring_assignments`) ; la fusion
  de 3+ sections homonymes peut dupliquer des pivots `course_group` (l'agent Fable
  avait validé cette migration — la contre-lecture a tranché pour Sol) ; l'import
  est sensible à la casse là où MySQL ne l'est pas (500 / lignes ignorées,
  → [IFO-014](../tickets/IFO-014-import-sensible-casse.md)) ; MySQL local publié
  sur `0.0.0.0` avec identifiants connus ; nuance sur la sonde : retirer le mot de
  passe de la commande de healthcheck ne le retire pas de `Config.Env` dans
  `docker inspect` — la justification du 2026-08-21 était partiellement surévaluée.

## Correctifs appliqués dans la foulée

- `bootstrap/app.php` : `trustProxies(at: '*')` ; compose Coolify :
  `SESSION_SECURE_COOKIE=true` par défaut et `LOG_CHANNEL=stderr`.
- Garde-fous de suppression : impossible de supprimer son propre compte via
  `/admin/users` (la zone de danger affiche la raison), impossible de supprimer le
  dernier compte via la page profil. Tests ajoutés, test existant adapté.
- `Dockerfile` / `Dockerfile.dev` : `php.ini` production/development activé +
  `docker/php.ini` (upload 300 Mo, post 310 Mo, mémoire 512 Mo) ; healthcheck sur
  `/up` (ne crée plus une session en base toutes les 30 s).
- Migrations : garde anti-résurrection sur `create_recurring_assignments`
  (ancien nom déjà en table `migrations` → no-op) ; fusion `course_group` réécrite
  par insertion dédoublonnée.
- Stacks locales : ports MySQL liés à `127.0.0.1`.
- Docs : compteurs STATUS (274/1094, quatorze bugs), port dev 8001 dans README et
  CLAUDE.md, guide Coolify (dump via `MYSQL_ROOT_PASSWORD` dans le conteneur, gel de
  tous les mots de passe MySQL à l'init du volume, politique de mot de passe non
  appliquée par `app:create-admin-user`).

**À noter pour le prochain déploiement Coolify** : rien à changer dans l'interface
(les nouveaux défauts de compose suffisent), mais un redéploiement est nécessaire
pour reconstruire l'image (php.ini, healthcheck) et recharger l'environnement.

**Validation de l'image reconstruite** (stack prod-like locale, 2026-08-22) :
conteneur `healthy` sur la sonde `/up`, `/`, `/screen`, `/screen/data`, `/login`
en 200, migrations — dont les deux corrigées — passées sur MySQL 8.4, compte admin
créé, `php -i` confirme `upload_max_filesize=300M`, `post_max_size=310M`,
`memory_limit=512M` et le `php.ini` de production chargé. Vérifié aussi via l'API
Coolify : l'application tourne `running:healthy` sur `https://display.choboai.com`
(HTTPS, `redirect: both`) — `SESSION_SECURE_COOKIE=true` est donc sans risque.

Au passage, deux pièges des stacks locales constatés et consignés dans IFO-015 :
les deux stacks partagent le nom de projet compose (ne pas les lancer en même
temps), et `app`/`vite` se disputent `composer install` au premier boot de la
stack dev (relancer `vite` suffit).

## Traductions (IFO-012)

Pages settings (profil / mot de passe / apparence), modal de suppression de compte,
confirmation de mot de passe, menu utilisateur, onglets Clair/Sombre/Système, et
chaînes `sr-only` des composants ui (Fermer, Plus, barre latérale, menu de
navigation). Hors périmètre assumé : le « Welcome » de la miniature de slide
(reflète le rendu TV réel) et les composants 2FA morts (IFO-015).

## Règles à retenir

- **Un seul auditeur ne suffit pas** : Fable a validé la migration de fusion que Sol
  a démontée ; la contre-lecture orchestrateur a départagé. Croiser puis vérifier
  soi-même reste la règle.
- **Un renommage de migration est une migration** : Laravel identifie par nom de
  fichier ; renommer = créer une « nouvelle » migration aux yeux des bases déjà
  passées.
- **Sans `php.ini`, PHP ment** : l'image officielle ne charge aucun ini ; les
  limites compilées (2 Mo) s'appliquent silencieusement et la validation Laravel ne
  voit jamais le fichier.

## Seconde vague (après-midi) — feu vert de Thibault, tests réels lundi

Sur demande explicite (« lets go tu peux corriger », « je te laisse faire les
changements de durcissement de IFO-015 ») :

- **[IFO-014](../tickets/IFO-014-import-sensible-casse.md) corrigé** : clé de
  correspondance `mb_strtolower` commune à l'aperçu, aux conflits, à la sélection
  et à l'exécution ; tables chargées entièrement pour ne plus dépendre de la
  collation du moteur. En bonus : 422 explicite sur fichier illisible, lecteurs
  PhpSpreadsheet restreints à Xlsx/Xls (sinon IOFactory retombe sur le lecteur
  CSV qui « lit » n'importe quoi). Six tests.
- **[IFO-015](../tickets/IFO-015-durcissements-differes.md) : 12 points sur 14**,
  dont le passage du conteneur en `www-data` (entrypoint en deux temps root →
  `setpriv`, capacité `net_bind_service` sur le binaire frankenphp), les en-têtes
  de sécurité Caddy, le throttle + la sérialisation stricte de `/screen/data`,
  `AuthenticateSession` (avec rafraîchissement du hash pour la session qui change
  son propre mot de passe), le verrou de `ensureDefaultSlides()`, le 422 sur
  course de créneau (testé en simulant le concurrent dans un hook `creating`),
  la suppression des reliquats 2FA (le seul `v-html` du projet disparaît, la
  suite passe de 277+7 ignorés à 284 passés/0 ignoré), un job CI MySQL 8.4 + un
  job de build d'image, et l'assainissement des stacks locales (noms de projet
  distincts, `.env` de dev sur MySQL, fin de la course composer app/vite).
  Deux restes assumés, consignés dans le ticket.
- **[IFO-016](../tickets/IFO-016-favicon-og-appname.md)** : favicons régénérés
  depuis la flèche jaune sur le navy de la marque (scripts rejouables), carte de
  partage OG 1200×630, nom d'application en dur — l'interface Coolify refuse les
  variables contenant des espaces, `APP_NAME` y est inutilisable.

Validation de la seconde vague : suite complète verte (**284 passés,
1133 assertions**), Pint/ESLint/vue-tsc/Prettier à zéro, stack prod-like
reconstruite et rejouée — PID 1 en `www-data` sur des volumes contenant des
fichiers créés en root, routes en 200, en-têtes présents, écritures storage OK.

## Pièges du jour (seconde vague)

- **PhpSpreadsheet ne refuse rien** : sur un fichier illisible, IOFactory bascule
  silencieusement sur le lecteur CSV → planning vide « réussi ». Toujours
  restreindre les lecteurs.
- **`AuthenticateSession` déconnecte aussi l'auteur du changement** de mot de
  passe si on ne remet pas `password_hash_web` à jour dans sa session.
- **Les `<env>` de phpunit.xml ne surchargent pas les vraies variables
  d'environnement** : injecter `DB_CONNECTION=mysql` dans le conteneur de dev
  aurait fait tourner les tests sur la base de développement (et
  `RefreshDatabase` l'aurait vidée). D'où le choix de passer par le `.env`
  généré, que Dotenv ne fait pas primer sur phpunit.

## Mise en production (fin de journée)

PR #4 fusionnée par Thibault ; le webhook a déclenché le déploiement Coolify
(`t14as6ft…`, ~5 min de build), surveillé via l'API read-only : **`finished`**,
application **`running:healthy`**. Vérifications depuis l'extérieur sur
`https://display.choboai.com` :

- `/up`, `/login`, `/screen`, `/screen/data`, `/og-image.png`, `/favicon.svg`
  tous en 200 ;
- cookie de session `secure; httponly; samesite=lax` — le correctif principal de
  l'audit est effectif ;
- `X-Frame-Options: DENY`, `nosniff`, `Referrer-Policy`, HSTS présents ;
- balises OG en URL absolues https ;
- logs de boot propres (« Nothing to migrate » — la base de prod était déjà à
  jour, les migrations corrigées y sont no-op comme prévu) et désormais
  **visibles dans l'onglet Logs de Coolify** (`stderr`).

CI de main verte, premiers passages de `ci-mysql` et `docker-image` compris.

Le compte admin de prod existe déjà : Thibault peut vider `ADMIN_EMAIL` /
`ADMIN_PASSWORD` dans l'interface — vérifié dans `CreateAdminUser` : variables
vides ⇒ « Saut de l'étape », sortie en succès, aucun déploiement cassé. À ne
re-renseigner que si le volume MySQL repart de zéro.

## À reprendre

Voir `docs/STATUS.md`, section « Prochaine action » — il reste la recette de
production (IFO-002), premiers tests réels prévus lundi.
