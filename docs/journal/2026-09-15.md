# 2026-09-15

## Fait

- **Import du planning annuel trop long**
  ([IFO-024](../tickets/IFO-024-import-planning-trop-long.md),
  [ADR-004](../decisions/ADR-004-import-planning-temps-execution.md)) :
  « Maximum execution time of 30 seconds exceeded » sur le fichier V3
  (quelques milliers d'attributions). Cause : une requête SQL par ligne à
  l'aperçu et à l'import, parsing PhpSpreadsheet exécuté deux fois, le tout
  dans la requête HTTP sous la limite de php.ini-production. Décision de
  Thibault : relever la limite maintenant (`max_execution_time = 300` +
  `set_time_limit(300)`), import en tâche de fond plus tard. Poussé sur
  `main`. `docs/data/` (fichiers Excel réels) ajouté au `.gitignore`.
- **Consignation de la veille** : [ADR-003](../decisions/ADR-003-kiosque-sans-cache-media.md)
  (kiosque sans cache média, mode debug embarqué, push direct sur `main`).
- Thibault a sorti la TV du mode debug et importé les vraies données en prod.
- Question de Thibault sur le rafraîchissement du kiosque : les données sont
  redemandées au chargement et à chaque retour sur la slide Bienvenue (une
  fois par boucle), jamais au milieu d'un tour ; un deploy ne recharge pas la
  page (nouveau code seulement après rechargement manuel). Comportement jugé
  bon, gardé tel quel.

## À faire

- Thibault : réessayer l'import du fichier V3 après déploiement.
- Plus tard : chantier « import en tâche de fond » (ADR-004), avec
  préchargement des attributions + `upsert` par paquets et parsing mémorisé.
